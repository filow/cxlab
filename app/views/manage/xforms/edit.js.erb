var node = $('#myModal')
node.find('#myModalLabel').html("编辑填表项目<%= @manage_xform.name %>");
node.find('.modal-body').html("<%= escape_javascript(render 'form') %>")


node.modal({keyboard:false,backdrop:'static'});